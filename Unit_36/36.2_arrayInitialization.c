#include <stdio.h>

int main()
{
    int numArr[10] = { 0, };      // 모두 0으로 초기화

    printf("%d\n", numArr[0]);    // 0
    printf("%d\n", numArr[5]);    // 0
    printf("%d\n", numArr[9]);    // 0

    return 0;
}
